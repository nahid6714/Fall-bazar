# Windows build

Requirements: Node.js 20+ and npm.

```powershell
npm install
npm run build
npm run dist
```

Installer: `release/Fol-Bazar-Admin-Setup-1.0.0.exe`

The build must be run on Windows for the Windows installer. macOS and Linux packages should be built on their respective platforms (or via CI).
