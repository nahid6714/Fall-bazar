# Fol Bazar — No Preview Popup UI

- Preview cards/sections are removed from the admin workflow.
- Banner/product/user/order information is opened directly as a page-style screen.
- No overlay/backdrop popup presentation remains for detail screens.
- Product image upload keeps a normal bounded image preview only, so the selected image can still be verified.
