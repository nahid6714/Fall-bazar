# User Filter Fix V4

Fixed the Customers screen filters.

- `সব`: searches name, phone, and Gmail when text is entered.
- `নাম`: filters to users with a name; with text, searches only the name.
- `নম্বর`: filters to users with a phone number; with text, searches only the phone number.
- `Gmail`: filters to users with an email; with text, searches only the email.
- The result count now reflects the active filter/search.
- Search label and placeholder update with the selected field.
