<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Matches context.getExternalFilesDir(Environment.DIRECTORY_DOWNLOADS), where
         ApkUpdateDownloader saves the update .apk before handing it to the installer. -->
    <external-files-path name="update_downloads" path="Download/" />
</paths>
